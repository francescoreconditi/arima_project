# setup.py - Mantenuto per compatibilità pip legacy
# Il file principale di configurazione è ora pyproject.toml (standard moderno)
# Per nuove installazioni usare: uv sync --all-extras

from setuptools import setup

# La configurazione principale è in pyproject.toml
# Questo file è mantenuto solo per compatibilità con strumenti legacy
setup()